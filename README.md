# Basic Snap-Framework Template

A stripped down version of `snap init`. Simply clone and `rm -rf .git` to start a new project.

## Rename Project

Simply replace `snap-basic-template` in `snap-basic-template.cabal`. 